# ============================================
# NanoClaw 环境变量配置模板
# ============================================
# 使用方法：复制此文件为 .env 并填入真实值
# cp .env.template .env
#
# 注意：.env 文件包含敏感信息，已在 .gitignore 中排除
# ============================================

# --------------------------------------------
# Claude Code 认证 (智谱 AI)
# 链接：https://www.bigmodel.cn/glm-coding?ic=B9FBAXMVQJ     
# 使用我这个链接注册, 比你直接自己买应该打个折，应该少几十块钱
# --------------------------------------------
ANTHROPIC_AUTH_TOKEN=your-api-key-here
ANTHROPIC_BASE_URL=https://open.bigmodel.cn/api/anthropic

# --------------------------------------------
# 飞书机器人配置
# --------------------------------------------
# 获取方式：
# 1. 访问 https://open.feishu.cn/
# 2. 创建企业自建应用
# 3. 在「凭证与基础信息」页面获取 App ID 和 App Secret
# 4. 在「事件与回调」中配置事件订阅
FEISHU_APP_ID=your-feishu-app-id
FEISHU_APP_SECRET=your-feishu-app-secret

# 消息长度阈值（可选）
# 当回复内容超过此字符数时，自动创建飞书文档并发送链接
# 飞书单条消息限制约 4000-5000 字符
# 默认: 4000 字符
# 需要权限：docx:document, docx:document:share
FEISHU_DOC_THRESHOLD=4000

# 飞书文档配置（可选）
# FEISHU_ADMIN_USER_ID: 您的飞书用户 ID，创建文档后自动添加您为管理员
# 文档会自动按 Plans/{群组名称}/ 结构组织存储
# FEISHU_ADMIN_USER_ID=ou_xxxxxxxxx

# --------------------------------------------
# 助手配置 (可选)
# --------------------------------------------
# 助手名称，用于触发消息 (默认: Andy)
ASSISTANT_NAME=Andy

# --------------------------------------------
# 容器配置 (可选)
# --------------------------------------------
# 容器镜像名称 (默认: nanoclaw-agent:latest)
# CONTAINER_IMAGE=nanoclaw-agent:latest

# 容器超时时间，毫秒 (默认: 1800000 = 30分钟)
# CONTAINER_TIMEOUT=1800000

# 容器最大输出大小，字节 (默认: 10485760 = 10MB)
# CONTAINER_MAX_OUTPUT_SIZE=10485760

# 模型映射 (可选) - 将 Claude 模型映射到其他模型
# 使用智谱 AI 等兼容服务时可配置
# ANTHROPIC_DEFAULT_HAIKU_MODEL=glm-5
# ANTHROPIC_DEFAULT_OPUS_MODEL=glm-5
# ANTHROPIC_DEFAULT_SONNET_MODEL=glm-5

# --------------------------------------------
# 运行时配置 (可选)
# --------------------------------------------
# 时区设置 (默认: 系统时区)
# TZ=Asia/Shanghai

# 日志级别: debug, info, warn, error (默认: info)
# LOG_LEVEL=info

# 空闲超时时间，毫秒 (默认: 1800000 = 30分钟)
# IDLE_TIMEOUT=1800000

# 最大并发容器数 (默认: 5)
# MAX_CONCURRENT_CONTAINERS=5
